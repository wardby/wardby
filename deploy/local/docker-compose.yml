services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: reevo
      POSTGRES_PASSWORD: reevo
      POSTGRES_DB: reevo
    ports:
      - "55432:5432"
    volumes:
      - reevo-postgres-data:/var/lib/postgresql/data

volumes:
  reevo-postgres-data:
