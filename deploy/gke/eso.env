# External Secrets Operator chart, pinned. Sourced by up.sh and verify-eso-kind.sh
# so both install exactly the same thing. Upgrade by changing the version here
# and re-running verify-eso-kind.sh.
ESO_CHART_REPO="https://charts.external-secrets.io"
ESO_CHART_VERSION="2.11.0"
