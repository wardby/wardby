# Applied by deploy/gke/up.sh before the main gke-autopilot overlay, after the
# namespace exists and ESO is installed, so the Secrets are Ready before any
# Deployment starts. Placeholders (wardby-gcp-project, wardby-cluster-location,
# wardby-cluster-name, wardby-secret-prefix) are substituted at apply time.
resources:
  - store.yaml
  - external-secrets.yaml
